import { createStitches } from "@stitches/react";


export const {config, styled, css, globalCss, keyframes, getCssText, createTheme, theme} = createStitches({
    theme: {
        colors:{
            rocketseat: '#8257e6',
        }
    }
})